root@vishaalnoord7:/opt/dataguardian# cat /etc/nginx/nginx.conf
events {
    worker_connections 1024;
    include /etc/nginx/sites-enabled/*;
}
http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    gzip on;
    upstream dataguardian_app {
        server 127.0.0.1:5000;
    }
    upstream webhook_server {
    map $http_upgrade $connection_upgrade { default upgrade; '' close; }
        server 127.0.0.1:5001;
    }
    server {
        listen 80;
        server_name dataguardianpro.nl www.dataguardianpro.nl 45.81.35.202;
        client_max_body_size 100M;
        location /.well-known/acme-challenge/ {
            root /var/www/html;
        }
        location /health {
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
        location /webhook/ {
            proxy_pass http://webhook_server;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Stripe-Signature $http_stripe_signature;
            proxy_buffering off;
        }
        location / {
            proxy_pass http://dataguardian_app;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 86400;
        }
    }
    include /etc/nginx/sites-enabled/*;
}
root@vishaalnoord7:/opt/dataguardian# cat /etc/nginx/sites-available/dataguardianpro.nl
server {
    listen 80;
    listen [::]:80;
    server_name dataguardianpro.nl www.dataguardianpro.nl;

    # Redirect all HTTP to HTTPS
    return 301 https://$server_name$request_uri;
}

# HTTPS server
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name dataguardianpro.nl www.dataguardianpro.nl;

    # SSL Certificate
    ssl_certificate /etc/letsencrypt/live/dataguardianpro.nl/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/dataguardianpro.nl/privkey.pem; # managed by Certbot

    # SSL Configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;

    # Proxy to Streamlit
    location /webhook/ { proxy_pass http://127.0.0.1:5001; proxy_buffering off; }
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 300s;
    }


}